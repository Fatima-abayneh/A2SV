class Solution:
    def heightChecker(self, heights: List[int]) -> int:
        c = 0
        nheight = sorted(heights)
        
        
        for i in range(len(heights)):
            if nheight[i] != heights[i]:
                c+=1
        return c
        
