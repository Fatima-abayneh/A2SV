class Solution(object):
    def sortedSquares(self, nums):
        """
        :type nums: List[int]
        :rtype: List[int]
        """
        temp=[]
        for i in nums:
            temp.append(i**2)
        temp.sort()
        return temp
